<UserControl x:Class="FuwanViewer.Presentation.Views.VisualNovelPreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:FuwanViewer.Presentation.ViewModels"
             xmlns:converters="clr-namespace:FuwanViewer.Presentation.ValueConverters"
             mc:Ignorable="d"
             d:DesignHeight="150" d:DesignWidth="500" 
             d:DataContext="{d:DesignInstance Type={x:Type vm:StubVisualNovelPreviewViewModel}, IsDesignTimeCreatable=True}">
    
    <UserControl.Resources>
        <converters:TagListToStringConverter x:Key="myCollectionToStringConverter"/>

        <Style x:Key="Title" TargetType="TextBlock">
            <Setter Property="Margin" Value="2"></Setter>
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontFamily" Value="/FuwanViewer.Presentation;component/Resources/Fonts/#Tikal Sans Medium" />
        </Style>

        <Style x:Key="Tags" TargetType="TextBlock">
            <Setter Property="Margin" Value="1"></Setter>
            <Setter Property="FontSize" Value="10" />
            <Setter Property="FontFamily" Value="/FuwanViewer.Presentation;component/Resources/Fonts/#Tikal Sans Medium" />
        </Style>

        <Style x:Key="Description" TargetType="TextBlock">
            <Setter Property="Margin" Value="1"></Setter>
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontFamily" Value="/FuwanViewer.Presentation;component/Resources/Fonts/#Tikal Sans Medium" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <DockPanel Grid.Column="0" Grid.Row="0">  
            <Border  Margin="10" BorderThickness="2" BorderBrush="#FFFFFFFF">
                <Image DockPanel.Dock="Left" Source="{Binding Cover}" RenderOptions.BitmapScalingMode="HighQuality"/>
            </Border>

            <Grid d:DataContext="{StaticResource DesignTimeDataContext}" 
            DockPanel.Dock="Top" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition ></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>


                <TextBlock Grid.Row="0"
                Style="{StaticResource Title}"
            Text="{Binding Title}"
            TextAlignment="Center"
            FontSize="20"
            Margin="10"/>


                <TextBlock  Grid.Row="1"
                Style="{StaticResource Tags}"
            Text="{Binding Path=Tags, Converter={StaticResource myCollectionToStringConverter}}"
            TextWrapping="Wrap"
            Margin="5"/>

                <ScrollViewer Grid.Row="2">

                    <TextBlock  
                Style="{StaticResource Description}"
            Text="{Binding Description}" 
            TextWrapping="Wrap"
            Margin="5"/>
                </ScrollViewer>


            </Grid>

        </DockPanel>
    </Grid>
</UserControl>
